# 项目当前状态（2026-05-27 更新）

> **本文档是项目唯一参考源。** 已整合 `技术栈.txt`、`功能结构.txt`、`开发顺序.md` 的全部关键信息。
> 每次开发完成后请更新对应模块的状态。

---

## 一、系统定位

一个本地运行的、面向 Excel 一对多结构的数据清洗工具。
对标 OpenRefine 但更专注，所有数据不出本地，适合涉密数据场景，无需公式知识即可操作。

---

## 二、技术栈（定稿）

| 层 | 选型 | 理由 |
|----|------|------|
| | 桌面壳 | Tauri v2 | 原生窗口，<10MB，跨平台 |
| | 前端框架 | React 19 + TypeScript | 生态最成熟 |
| | 表格组件 | AG Grid 社区版 | 虚拟滚动、合并单元格、筛选排序 |
| | 样式 | Tailwind CSS v4 | 零心智负担 |
| | Excel 读写 | exceljs (MIT) | MIT 协议无顾虑 |
| | CSV 解析 | papaparse | 高性能 |
| | 状态管理 | Zustand | 比 Redux 轻一档 |
| | 构建工具 | Vite | Tauri 官方推荐 |
| | 包管理 | pnpm | 快、稳 |
| | 日期处理 | date-fns | Phase 3 用 |
| | 测试 | Vitest | 与 Vite 无缝集成 |

**关键约束：**
- 所有数据操作逻辑在主线程执行（WebWorker 已预留协议但未启用，后续可切）
- Tauri 的 Rust 侧不写任何业务逻辑，只负责窗口 + 系统对话框 + 文件 I/O
- Phase 1 只支持 .xlsx + .csv，不支持 .xls（老格式）
- 不做的功能：PDF 导出、插件系统、数据可视化（图表）、打印

---

## 三、架构总图（当前实际 2026-05-27）

```
                        Tauri Window
                             │
                 ┌───────────┴───────────┐
                 │     React App          │  ← App.tsx
                 │  ┌──────────────────┐  │
                 │  │  顶栏 (Header)   │  │  ← Undo/Redo + 导入/导出（内联，无独立 ToolBar）
                 │  ├────────┬─────────┤  │
                 │  │DataGrid│Operation│  │  ← 左右分栏
                 │  │(AG Grid│ Panel   │  │     DataGrid.tsx + OperationPanel.tsx
                 │  │ 表格)  │(右侧栏) │  │     （仅导入后显示 OperationPanel）
                 │  ├────────┴─────────┤  │
                 │  │    StatusBar     │  │  ← 底部统计栏（components/StatusBar.tsx）
                 │  ├──────────────────┤  │
                 │  │  ConfirmDialog   │  │  ← 全局确认弹窗（components/ConfirmDialog.tsx）
                 │  └──────────────────┘  │
                 │          │              │
                 │  ┌───────┴───────┐      │
                 │  │   Zustand     │      │
                 │  │   Stores      │      │
                 │  │  - tableStore │      │
                 │  │  - historyStore│     │
                 │  │  - confirmStore│     │
                 │  └───────┬───────┘      │
                 │          │              │
                 │  ┌───────┴───────┐      │
                 │  │  Engine 层     │      │
                 │  │ (Operation 模式)│     │  ← engine/operations/
                 │  │ + execute hook │      │     useOperationExecutor
                 │  └───────────────┘      │
                 └──────────────────────────┘
```

### UI 组件状态（更新）

| 组件 | 文件 | 状态 | 说明 |
|------|------|------|------|
| App | `components/App.tsx` | ✅ 已更新 | 顶栏内置 Undo/Redo + 导入/导出，移除独立 ToolBar |
| DataGrid | `components/DataGrid.tsx` | ✅ | AG Grid 表格 |
| StatusBar | `components/StatusBar.tsx` | ✅ | 底部统计栏 |
| OperationPanel | `components/OperationPanel.tsx` | ✅ | 右侧操作面板，按功能结构分组，含删除空行/按列合并弹窗 |
| ConfirmDialog | `components/ConfirmDialog.tsx` | ✅ | 全局确认弹窗，监听 confirmStore |
| HistorySidebar | `components/HistorySidebar.tsx` | ❌ 待建 | 操作历史时间线面板 |
| **ToolBar** | `components/ToolBar.tsx` | ❌ 已弃用 | 操作按钮已迁移至 OperationPanel（保留文件，不再被 App 引用） |

**Workspace 概念预留（Phase 2）：**
- `Workspace.tables: Map<string, NormalizedTable>`（类型已定义）
- 跨表操作在当前单表模式下不启用

---

## 四、核心类型

> 所有类型定义在 `engine/types.ts`，这是唯一标准。
> `types/index.ts` 仅保留 ImportOptions/ExportOptions 等工具类型，主链路已不依赖。

```typescript
type ColumnType = 'text' | 'integer' | 'float' | 'currency' | 'percentage' | 'date' | 'time' | 'datetime'
type SemanticTag = 'phone' | 'email' | 'idcard' | 'bankcard'

interface Column {
  id: string
  name: string
  dtype: ColumnType
  semanticTag?: SemanticTag
  isKey: boolean
}

interface Cell {
  value: string | number | null
  isVirtual: boolean
  sourceRef: string | null
}

interface Row {
  id: number
  isVirtual: boolean
  cells: Cell[]
}

interface NormalizedTable {
  columns: Column[]
  rows: Row[]
  originalFileName: string
  sheetName: string
}
```

### 数据流（已统一）

```
文件 → 导入器(parseXLSX/parseCSV) → NormalizedTable → tableStore → AG Grid 展示
                                            ↓
                                     Engine Operation.execute()
                                            ↓
                                   返回新的 NormalizedTable → tableStore.updateTable()
                                            ↓
                                    导出器(exportToXLSX/exportToCSV) → 文件
```

---

## 五、已完成的模块

### 5.1 导入模块 ✅
| 文件 | 功能 |
|------|------|
| `importers/xlsxImporter.ts` | `parseXLSX(buffer, fileName) → NormalizedTable` |
| `importers/csvImporter.ts` | `parseCSV(text, fileName, options) → NormalizedTable` |

### 5.2 导出模块 ✅
| 文件 | 功能 |
|------|------|
| `exporters/xlsxExporter.ts` | `exportToXLSX(table) → ArrayBuffer` |
| `exporters/csvExporter.ts` | `exportToCSV(table, options) → string` |
| `utils/saveFile.ts` | `saveFile()` — Tauri 原生保存对话框 + 浏览器下载 |

### 5.3 操作引擎 ✅ — Command Pattern
```typescript
abstract class BaseOperation {
  abstract readonly type: string
  abstract readonly label: string
  abstract readonly detail: string
  abstract execute(table: NormalizedTable): NormalizedTable
  abstract undo(table: NormalizedTable): NormalizedTable
  abstract serialize(): SerializedOperation
  protected cloneTable(table): NormalizedTable  // structuredClone
}
```

已实现的操作（13个）：

| 操作类 | 文件 | 功能 | Phase |
|--------|------|------|-------|
| `FillDownOp` | `engine/operations/FillDownOp.ts` | 合并单元格向下填充 | P1 |
| `VisualCollapseOp` | `engine/operations/VisualCollapseOp.ts` | 视觉合并（FillDown 纯视图逆操作，不删行，用 isVirtual+sourceRef 标记） | P1 ✅ 新增 |
| `ExplodeOp` | `engine/operations/ExplodeOp.ts` | 按分隔符/换行符展开行 | P1 |
| `CollapseOp` | `engine/operations/CollapseOp.ts` | 按列合并（Explode 逆操作，含拼接弹窗） | P1 |
| `TrimOp` | `engine/operations/TrimOp.ts` | 去前后空格 | P1 ✅ 新增 |
| `CleanInvisibleOp` | `engine/operations/CleanInvisibleOp.ts` | 清除不可见字符（零宽字符、特殊空白、控制字符） | P1 ✅ 新增 |
| `NormalizeNewlineOp` | `engine/operations/NormalizeNewlineOp.ts` | 规范化换行符（\r\n/\r → \n，合并连续空行） | P1 ✅ 新增 |
| `RemoveEmptyOp` | `engine/operations/RemoveEmptyOp.ts` | 清除空行/空列（严格模式/关键列模式 + OR/AND 逻辑 + 空白视为空开关） | P1 ✅ 新增 |
| `NormalizeWidthOp` | `engine/operations/NormalizeWidthOp.ts` | 全角转半角（字母/数字/标点） | P1 ✅ 新增 |
| `RegexReplaceOp` | `engine/operations/RegexReplaceOp.ts` | 正则替换（按列/全局） | P1 ✅ 新增 |
| `DedupeOp` | `engine/operations/DedupeOp.ts` | 按列去重（保留第一条/最后一条） | P1 ✅ 新增 |
| `SplitColumnOp` | `engine/operations/SplitColumnOp.ts` | 拆分列（按分隔符/固定宽度/正则） | P1 ✅ 新增 |
| `AddColumnOp` | `engine/operations/AddColumnOp.ts` | 新增列（固定值/序号/公式） | P2 ✅ 新增 |

操作注册表：`engine/operations/registry.ts`
注册入口：`engine/operations/index.ts`

### 5.4 状态管理 ✅
- **tableStore** (`stores/tableStore.ts`)：当前表格 NormalizedTable + loading/error 状态
- **historyStore** (`stores/historyStore.ts`)：undo/redo 栈（存序列化 Operation，不存全量快照）
- **confirmStore** (`stores/confirmStore.ts`)：全局确认弹窗状态 ✅ 新增

### 5.5 UI 组件 ✅
| 组件 | 文件 | 状态 |
|------|------|------|
| App | `App.tsx` | ✅ 顶栏内置 Undo/Redo + 导入/导出 |
| DataGrid | `components/DataGrid.tsx` | ✅ |
| StatusBar | `components/StatusBar.tsx` | ✅ |
| OperationPanel | `components/OperationPanel.tsx` | ✅ 按功能结构分组（内容清洗/结构整理/行列操作/去重与合并/格式标准化），含删除空行弹窗、按列合并弹窗 |
| ConfirmDialog | `components/ConfirmDialog.tsx` | ✅ |
| ExportConfirmModal | `App.tsx` 内联 | ✅ 导出格式选择弹窗 |

### 5.6 Hooks ✅
- `useOperationExecutor` (`hooks/useOperationExecutor.ts`)：execute/undo/redo 三合一

### 5.7 Tauri 集成 ✅
- `src-tauri/src/lib.rs`：仅窗口管理
- `utils/saveFile.ts`：原生保存对话框

### 5.8 公式引擎框架 ✅（Phase 3 提前搭建）
| 文件 | 功能 |
|------|------|
| `engine/formulas/types.ts` | 公式类型定义（AST 节点、Token 类型） |
| `engine/formulas/parser.ts` | 公式解析器（Tokenize → Parse → AST） |
| `engine/formulas/functions.ts` | 内置函数注册表 |
| `engine/formulas/evaluator.ts` | AST 求值器 |
| `engine/formulas/index.ts` | 统一导出 |

> 当前仅完成架构搭建，尚未集成到 OperationPanel UI 和 AddColumnOp 中。
> 后续 AddColumnOp 的"自定义公式"模式将对接此引擎。

### 5.9 预留目录
| 目录 | 状态 | 用途 |
|------|------|------|
| `converters/` | 📁 空目录 | 预留：格式标准化转换器（格式标准化操作已直接放在 engine/operations/ 下） |

---

## 六、未完成的模块（按功能树 A-Z）

### 🔴 A. 导入增强
| 编号 | 功能 | 说明 |
|------|------|------|
| A2 | 合并单元格自动检测 + Unmerge + FillDown | 导入时自动展开 |
| A3 | 智能多值列检测 | 采样前 100 行，扫描分隔符出现率 |
| A4 | 手动指定分隔规则 | 修正自动检测误判 |
| A5 | 多工作表选择 | Phase 1 选 Sheet，Phase 2 批量 |
| A6 | 导入错误报告 | 损坏文件/格式错误/跳过行 |
| A7 | 导入上限性能警告 | 超 5 万行弹提示，不设硬上限 |

### 🔴 B. 展开操作
| 编号 | 功能 | Engine | UI |
|------|------|--------|----|
| B1 | 按列展开（分隔符拆多行） | ✅ ExplodeOp | ✅ OperationPanel |
| B2 | 含换行符展开 | ✅ ExplodeOp | ✅ OperationPanel |
| B3 | 多列同步展开（按位置匹配） | ❌ | ❌ |
| B4 | 合并单元格展开 | ✅ FillDownOp | ✅ OperationPanel |
| B4-逆 | 视觉合并（FillDown 逆操作） | ✅ VisualCollapseOp | ✅ OperationPanel（紧挨 FillDown，图标 SplitCellsOutlined 旋转 90°） |
| B5 | 展开预览 | ❌ | ❌ |
| B6 | 按规则展开（固定长度/正则） | ❌ Phase 3 | ❌ |

### 🔴 C. 合并操作
| 编号 | 功能 | 说明 |
|------|------|------|
| C1 | 按列合并 | ✅ CollapseOp（Engine + UI 弹窗，含拼接列选择 + 分隔符配置） |
| C2 | 选中行合并 | ❌ |
| C3 | 多列聚合合并 | ❌ |
| C4 | 聚合计算合并 | ❌ Phase 3 |

### 🔴 D. 列操作
| 编号 | 功能 | 状态 |
|------|------|------|
| D1 | 添加列 | ✅ AddColumnOp（固定值/序号，公式模式待对接 formulas 引擎） |
| D2 | 删除列 | ❌ |
| D3 | 重命名列 | ❌ |
| D4 | 重新排序列 | ❌ |
| D5 | 拆分列（Text to Columns） | ✅ SplitColumnOp |
| D6 | 合并列（Concatenate） | ❌ |
| D7 | 列类型转换 + 语义标签设置 | ❌ |

### 🟡 E. 行操作（已基本覆盖）
| 编号 | 功能 | 状态 |
|------|------|------|
| E1 | 筛选（条件/正则/模糊） | ❌ |
| E2 | 排序（单列/多列） | ❌ |
| E3 | 去重（按列去重） | ✅ DedupeOp |
| E4 | 删除空行/填充空白 | ✅ RemoveEmptyOp |

### 🟢 F. 数据清洗（已基本覆盖）
| 编号 | 功能 | 状态 |
|------|------|------|
| F1 | Trim（去前后空格） | ✅ TrimOp |
| F2 | 清除不可见字符 | ✅ CleanInvisibleOp |
| F3 | 大小写/全角半角统一 | ✅ NormalizeWidthOp（全角转半角） |
| F4 | 正则替换（带实时预览） | ✅ RegexReplaceOp |
| F5 | 模糊分组（拼写聚类） | ❌ Phase 3 |

### 🟢 G. 操作管理
| 编号 | 功能 | 状态 |
|------|------|------|
| G1 | Undo/Redo（Ctrl+Z/Ctrl+Y） | ✅ 顶栏内置 |
| G2 | 侧边栏操作历史时间线 | ❌ |
| G3 | 导出操作脚本 | Phase 4 |

### 🟡 H. 导出增强
| 编号 | 功能 | 状态 |
|------|------|------|
| H1 | 导出 .xlsx | ✅（带格式选择弹窗） |
| H2 | 导出 .csv | ✅（带格式选择弹窗） |
| H3 | 清除文档元数据 | ❌ |
| H4 | 批量导出 | ❌ Phase 2 |

### 🟡 I. 安全
| 编号 | 功能 |
|------|------|
| I2 | 数据脱敏 |
| I3 | 密码加密导出 |
| I4 | 操作审计日志 |

### 🔵 J. 公式计算引擎（Phase 3）
| 编号 | 功能 |
|------|------|
| J1 | 时间计算（工时/日期差/日期加减，含 DayOffProfile） |
| J2 | 身份证解析（生日/性别/年龄） |
| J3 | 文本提取（手机号/邮箱/数字） |

### 🔵 K. 跨表操作（Phase 2）
| 编号 | 功能 |
|------|------|
| K1 | 表连接（VLOOKUP/XLOOKUP 替代） |
| K2 | 表合并（多表纵向拼接） |
| K3 | 表拆分（按列值拆多个文件） |

### 🔵 L. 工作区管理（Phase 2）
| 编号 | 功能 |
|------|------|
| L1 | 多表同开（标签页切换） |
| L2 | 表名管理 |
| L3 | 跨表引用关系图 |

### 🟡 Z. 辅助功能
| 编号 | 功能 | 状态 |
|------|------|------|
| Z1 | 统计信息面板 | ⚠️ StatusBar 简版 |
| Z2 | 快捷键覆盖 | ✅ Ctrl+Z/Y + 导出弹窗 |
| Z3 | 帮助内嵌 | ❌ |

---

## 七、开发阶段规划

### Phase 1 — MVP（当前 ✅ 核心骨架完成）
- [x] 导入（.xlsx + .csv）
- [x] 导出（.xlsx + .csv，带格式选择弹窗）
- [x] 操作引擎（Command Pattern + 13 个 Operation：
  Trim / CleanInvisible / NormalizeNewline / NormalizeWidth / RegexReplace / RemoveEmpty /
  Dedupe / SplitColumn / AddColumn / FillDown / VisualCollapse / Explode / Collapse）
- [x] Undo/Redo（顶栏内置）
- [x] OperationPanel（按功能结构 5 大分类，含删除空行弹窗、按列合并弹窗）
- [x] ConfirmDialog（全局确认弹窗）
- [x] StatusBar（底部统计栏：行数/列数/选中统计）
- [x] 公式引擎框架（parser / evaluator / functions / types）
- [ ] 展开预览（B5）
- [ ] 导入增强：合并单元格检测（A2）、多值列检测（A3/A4）

### Phase 2 — 填平核心操作缺口（P0 最小可行操作集）

| 序号 | 操作 | 分类 | 说明 |
|:--:|------|------|------|
| 1 | **SortOp** | 行列操作 | 按列排序（升序/降序），高频操作 |
| 2 | **FilterOp** | 高级筛选 | 条件筛选行（等于/包含/大于/小于/正则匹配） |
| 3 | **DeleteRowsOp** | 行列操作 | 按条件删除行（空行 / 指定列值为空） |
| 4 | **TransposeOp** | 结构整理 | 行列转置 |
| 5 | **NormalizeDateOp** | 格式标准化 | 日期格式统一（多种格式 → yyyy-MM-dd） |
| 6 | **MergeSameKeyOp** | 去重与合并 | 同值行合并（拼接/求和/求平均） |

### Phase 3 — 补齐剩余引擎（P1）

| 序号 | 操作 | 分类 |
|:--:|------|------|
| 7 | **RenameColumnOp** | 行列操作 — 批量重命名列名 |
| 8 | **HideColumnOp / ShowColumnOp** | 行列操作 — 隐藏/显示列 |
| 9 | **NormalizeNumberOp** | 格式标准化 — 千分位、小数位数统一 |
| 10 | **GroupByKeywordOp** | 结构整理 — 按关键字聚类成组 |
| 11 | **SplitToSheetsOp** | 结构整理 — 按类别拆分到多个 sheet |
| 12 | **FuzzyDedupeOp** | 去重与合并 — 模糊去重（相似度匹配） |
| 13 | **NormalizePhoneOp / NormalizeIdCardOp** | 格式标准化 — 手机号/身份证校验与规范化 |
| 14 | **ConditionalReplaceOp** | 高级筛选 — 条件替换（如"北京"→"北京市"） |
| 15 | **MultiConditionOp** | 高级筛选 — 多条件组合（AND/OR） |

### Phase 4 — 视图增强与交互（P2）

| 序号 | 功能 | 说明 |
|:--:|------|------|
| 16 | **HistorySidebar** | 操作历史时间线面板，支持点击恢复到指定步骤 |
| 17 | **冻结行/列** | AG Grid 原生支持，配置即可 |
| 18 | **列宽自适应** | 双击列边界自动适配内容宽度 |
| 19 | **拖拽排序列** | AG Grid 原生支持，启用即可 |

### Phase 5 — 远期
公式引擎接入 AddColumnOp、跨表操作（K+L）、数据脱敏（I2-I4）、操作脚本导出重放、大文件流式+分页加载、帮助文档

---

## 八、当前下一步任务（2026-05-27 更新 — P0→P1→P2 计划）

### P0（立即推进）—— 填平核心操作缺口

| 优先级 | 任务 | 分类 | 说明 |
|--------|------|------|------|
| 🔴 1 | **SortOp** | Engine | 按列排序（升序/降序），使用频率最高 |
| 🔴 2 | **FilterOp** | Engine | 条件筛选行（等于/包含/大于/小于/正则），高频操作 |
| 🔴 3 | **DeleteRowsOp** | Engine | 按条件删除行（空行删除 / 指定列值为空时删除） |
| 🔴 4 | **TransposeOp** | Engine | 行列转置 |
| 🔴 5 | **NormalizeDateOp** | Engine | 日期格式统一（多种格式 → yyyy-MM-dd） |
| 🔴 6 | **MergeSameKeyOp** | Engine | 同值行合并（拼接/求和/求平均），分组汇总刚需 |

### P1（随后）—— 补齐剩余引擎

| 优先级 | 任务 | 分类 | 说明 |
|--------|------|------|------|
| 🟡 7 | **RenameColumnOp** | Engine | 批量重命名列名 |
| 🟡 8 | **HideColumnOp / ShowColumnOp** | Engine | 隐藏/显示列 |
| 🟡 9 | **NormalizeNumberOp** | Engine | 千分位、小数位数统一 |
| 🟡 10 | **GroupByKeywordOp** | Engine | 按关键字聚类成组 |
| 🟡 11 | **SplitToSheetsOp** | Engine | 按类别拆分到多个 sheet |
| 🟡 12 | **FuzzyDedupeOp** | Engine | 模糊去重（相似度匹配） |
| 🟡 13 | **NormalizePhoneOp / NormalizeIdCardOp** | Engine | 手机号/身份证校验与规范化 |
| 🟡 14 | **ConditionalReplaceOp** | Engine | 条件替换（如"北京"→"北京市"） |
| 🟡 15 | **MultiConditionOp** | Engine | 多条件组合（AND/OR） |

### P2（随后）—— 视图增强与交互

| 优先级 | 任务 | 分类 | 说明 |
|--------|------|------|------|
| 🟢 16 | **HistorySidebar** | UI | 操作历史时间线面板，支持点击恢复到指定步骤 |
| 🟢 17 | 冻结行/列 | UI | AG Grid 原生支持，配置即可 |
| 🟢 18 | 列宽自适应 | UI | 双击列边界自动适配内容宽度 |
| 🟢 19 | 拖拽排序列 | UI | AG Grid 原生支持，启用即可 |

---

## 九、项目目录结构（当前实际 2026-05-27）

```
excel-normalizer/
├── index.html
├── package.json
├── pnpm-workspace.yaml
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── src-tauri/
│   ├── Cargo.toml
│   ├── capabilities/
│   ├── icons/
│   └── src/
│       ├── lib.rs               # 窗口管理
│       └── main.rs
├── public/
│   └── logo.svg
├── tests/
│   └── placeholder.test.ts
└── src/
    ├── App.tsx                   # 根组件（顶栏内置 Undo/Redo + 导入/导出）
    ├── App.css
    ├── main.tsx
    ├── vite-env.d.ts
    ├── assets/
    ├── components/
    │   ├── App.tsx               # 主布局：导入/导出动作 + DataGrid + OperationPanel + StatusBar
    │   ├── DataGrid.tsx          # AG Grid 表格
    │   ├── StatusBar.tsx         # 底部统计栏
    │   ├── OperationPanel.tsx    # 右侧操作面板，按功能结构 5 大分类分组，含弹窗
    │   ├── ConfirmDialog.tsx     # 全局确认弹窗
    │   └── ToolBar.tsx           # ⚠️ 已弃用（不再被 App 引用）
    ├── engine/
    │   ├── index.ts              # 操作入口（executeOp / undoOp）
    │   ├── types.ts              # NormalizedTable, Column, Row, Cell 等核心类型
    │   ├── formulas/
    │   │   ├── index.ts          # 统一导出
    │   │   ├── types.ts          # 公式类型定义（AST 节点、Token 类型）
    │   │   ├── parser.ts         # 公式解析器（Tokenize → Parse → AST）
    │   │   ├── functions.ts      # 内置函数注册表
    │   │   └── evaluator.ts      # AST 求值器
    │   └── operations/
    │       ├── index.ts          # 注册中心（OPERATION_REGISTRY）+ 统一导出
    │       ├── registry.ts       # registry 注册表
    │       ├── base.ts           # 基类（Operation 接口）
    │       ├── FillDownOp.ts     # 合并单元格向下填充
    │       ├── VisualCollapseOp.ts # ✅ 视觉合并（FillDown 逆操作）
    │       ├── ExplodeOp.ts      # 按分隔符/换行符展开行
    │       ├── CollapseOp.ts     # 按列合并（Explode 逆操作）
    │       ├── TrimOp.ts         # 去前后空格
    │       ├── CleanInvisibleOp.ts # 清除不可见字符
    │       ├── NormalizeNewlineOp.ts # ✅ 规范化换行符
    │       ├── RemoveEmptyOp.ts  # ✅ 清除空行/空列（含弹窗）
    │       ├── NormalizeWidthOp.ts # ✅ 全角转半角
    │       ├── RegexReplaceOp.ts # ✅ 正则替换
    │       ├── DedupeOp.ts       # ✅ 按列去重
    │       ├── SplitColumnOp.ts  # ✅ 拆分列
    │       └── AddColumnOp.ts    # 新增列（固定值/序号）
    ├── stores/
    │   ├── tableStore.ts         # table, loading, error, updateTable, markExecuting, clearExecuting
    │   ├── historyStore.ts       # undoStack / redoStack / push / undo / redo
    │   └── confirmStore.ts       # 全局确认弹窗状态
    ├── importers/
    │   ├── index.ts
    │   ├── xlsxImporter.ts
    │   └── csvImporter.ts
    ├── exporters/
    │   ├── index.ts
    │   ├── xlsxExporter.ts
    │   └── csvExporter.ts
    ├── hooks/
    │   └── useOperationExecutor.ts  # executeOp / undoOp / redoOp 三合一 hook
    ├── workers/                     # WebWorker 预留（已弃用，操作在主线程执行）
    │   ├── excelWorker.ts
    │   ├── workerClient.ts
    │   └── workerHandler.ts
    ├── utils/
    │   └── saveFile.ts
    ├── converters/                  # 📁 预留目录
    ├── styles/
    └── types/
        └── index.ts                 # ImportOptions / ExportOptions
```

### 待清理文件

| 文件/目录 | 处理方式 |
|-----------|----------|
| `src/operations/` | 旧引擎目录，逐步删除（功能已迁移至 engine/operations/） |
| `src/components/ToolBar.tsx` | 已弃用，可删除（操作入口在 OperationPanel + 顶栏） |
| `src/types/index.ts` | 仅保留工具类型，旧 NormalizedTable 定义已移至 engine/types.ts |

---

## 十、当前待办 CheckList（Cline 管理）

```
✅ Phase 1 Operations（13/13 完成）
  ✅ TrimOp           — 去前后空格
  ✅ CleanInvisibleOp — 清除不可见字符
  ✅ NormalizeNewlineOp — 规范化换行符
  ✅ RemoveEmptyOp    — 清除空行/空列（含弹窗：严格/关键列 + OR/AND）
  ✅ NormalizeWidthOp — 全角转半角
  ✅ RegexReplaceOp   — 正则替换
  ✅ FillDownOp       — 合并单元格向下填充
  ✅ VisualCollapseOp — 视觉合并（FillDown 逆操作）
  ✅ ExplodeOp        — 按分隔符展开
  ✅ CollapseOp       — 按列合并（含弹窗：拼接列+分隔符）
  ✅ SplitColumnOp    — 拆分列
  ✅ DedupeOp         — 按列去重
  ✅ AddColumnOp      — 新增列（固定值/序号/公式）
✅ OperationPanel UI — 5 大分类 + 删除空行弹窗 + 按列合并弹窗，两对逆操作紧挨排列

❌ Phase 2 P0 — 填平核心操作缺口（6个 Operation）
  ❌ SortOp          — 按列排序（升序/降序），高频刚需
  ❌ FilterOp        — 条件筛选行（等于/包含/大于/小于/正则匹配），高频刚需
  ❌ DeleteRowsOp    — 按条件删除行
  ❌ TransposeOp     — 行列转置
  ❌ NormalizeDateOp — 日期格式统一（多种格式 → yyyy-MM-dd）
  ❌ MergeSameKeyOp  — 同值行合并（拼接/求和/求平均）

❌ Phase 3 P1 — 补齐剩余引擎（9个 Operation）
  ❌ RenameColumnOp  — 批量重命名列名
  ❌ HideColumnOp / ShowColumnOp — 隐藏/显示列
  ❌ NormalizeNumberOp — 千分位、小数位数统一
  ❌ GroupByKeywordOp — 按关键字聚类成组
  ❌ SplitToSheetsOp — 按类别拆分到多个 sheet
  ❌ FuzzyDedupeOp   — 模糊去重（相似度匹配）
  ❌ NormalizePhoneOp / NormalizeIdCardOp — 手机号/身份证校验与规范化
  ❌ ConditionalReplaceOp — 条件替换（如"北京"→"北京市"）
  ❌ MultiConditionOp — 多条件组合（AND/OR）

❌ Phase 4 P2 — 视图增强与交互（4项）
  ❌ HistorySidebar  — 操作历史时间线面板
  ❌ 冻结行/列       — AG Grid 原生支持
  ❌ 列宽自适应      — 双击列边界
  ❌ 拖拽排序列      — AG Grid 原生支持
```

---

## 附录

### A. 为什么不用 Tauri 的 Rust 侧做数据操作
- 当前所有引擎操作都在 TypeScript 中完成，数据量 <10万行时主线程完全够用
- 如果数据特别大（>10万行），后续可以用 WebWorker 分担，TypeScript 调试成本远低于 Rust
- Tauri 的 Rust 侧只跑窗口+文件对话框，保持薄壳原则

### B. 为什么不用 Redux
- Zustand 对 React 18 支持更好
- boilerplate 减少 70%
- immer 集成自然，适合树状状态的不可变更新

### C. 为什么不用 NPM / Yarn
- pnpm 磁盘占用最低
- monorepo 支持最好
- lock file 最稳定

### D. 操作安全约定
```
execute 流程：
  tableStore.markExecuting()               // 禁止导出
    → op.execute(clone)                    // 在副本上执行（失败不影响原数据）
    → tableStore.updateTable(newTable)     // 成功则替换
    → historyStore.push(op.serialize())    // 记录可撤销
    → tableStore.clearExecuting()          // 恢复导出

注意事项：
  - Cell 值为 null 表示合并单元格中的被合并格
  - 合并单元格（Merged Cells）通过 Cell.sourceRef + Row.isVirtual 实现
```

### E. 开发规范

**文件注释格式：**
```typescript
/**
 * 模块名 — 一句话描述
 *
 * 职责：详细说明该模块的职责
 *
 * 数据流（如涉及）：
 *   Step 1 → Step 2 → Step 3
 */
```

**Operation 类实现标准：**
```typescript
export class XxxOp extends BaseOperation {
  readonly type = "xxx";
  readonly label = "操作名称";
  readonly detail = "操作参数描述";

  execute(table: NormalizedTable): NormalizedTable {
    const result = this.cloneTable(table);
    // ... 修改 result
    return result;
  }

  undo(table: NormalizedTable): NormalizedTable {
    // ... 逆向操作
    return table;
  }

  serialize(): SerializedOperation {
    return { type: this.type, params: { ... } };
  }
}
```

**Store 标准：**
```typescript
import { create } from "zustand";
interface XxxState { /* ... */ }
export const useXxxStore = create<XxxState>((set, get) => ({ /* ... */ }));
```

**Component 标准：**
```typescript
/**
 * 组件名 — 功能描述
 */
export function XxxComponent({ prop1, prop2 }: Props) { /* ... */ }