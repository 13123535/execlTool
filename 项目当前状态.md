# 项目当前状态（2026-05-26 更新）

> **本文档是项目唯一参考源。** 已整合 `技术栈.txt`、`功能结构.txt`、`开发顺序.md` 的全部关键信息。
> 每次开发完成后请更新对应模块的状态。

---

## 一、系统定位

一个本地运行的、面向 Excel 一对多结构的数据清洗工具。
对标 OpenRefine 但更专注，所有数据不出本地，适合涉密数据场景，无需公式知识即可操作。

---

## 二、技术栈（定稿）

| 层 | 选型 | 理由 |
|----|------|------|
| 桌面壳 | Tauri v2 | 原生窗口，<10MB，跨平台 |
| 前端框架 | React 19 + TypeScript | 生态最成熟 |
| 表格组件 | AG Grid 社区版 | 虚拟滚动、合并单元格、筛选排序 |
| 样式 | Tailwind CSS v4 | 零心智负担 |
| Excel 读写 | exceljs (MIT) | MIT 协议无顾虑 |
| CSV 解析 | papaparse | 高性能 |
| 状态管理 | Zustand | 比 Redux 轻一档 |
| 构建工具 | Vite | Tauri 官方推荐 |
| 包管理 | pnpm | 快、稳 |
| 日期处理 | date-fns | Phase 3 用 |
| 测试 | Vitest | 与 Vite 无缝集成 |

**关键约束：**
- 所有数据操作逻辑在主线程执行（WebWorker 已预留协议但未启用，后续可切）
- Tauri 的 Rust 侧不写任何业务逻辑，只负责窗口 + 系统对话框 + 文件 I/O
- Phase 1 只支持 .xlsx + .csv，不支持 .xls（老格式）
- 不做的功能：PDF 导出、插件系统、数据可视化（图表）、打印

---

## 三、架构总图（当前实际 2026-05-26）

```
                        Tauri Window
                             │
                 ┌───────────┴───────────┐
                 │     React App          │  ← App.tsx
                 │  ┌──────────────────┐  │
                 │  │  顶栏 (Header)   │  │  ← Undo/Redo + 导入/导出（内联，无独立 ToolBar）
                 │  ├────────┬─────────┤  │
                 │  │DataGrid│Operation│  │  ← 左右分栏
                 │  │(AG Grid│ Panel   │  │     DataGrid.tsx + OperationPanel.tsx
                 │  │ 表格)  │(右侧栏) │  │     （仅导入后显示 OperationPanel）
                 │  ├────────┴─────────┤  │
                 │  │    StatusBar     │  │  ← 底部统计栏（components/StatusBar.tsx）
                 │  ├──────────────────┤  │
                 │  │  ConfirmDialog   │  │  ← 全局确认弹窗（components/ConfirmDialog.tsx）
                 │  └──────────────────┘  │
                 │          │              │
                 │  ┌───────┴───────┐      │
                 │  │   Zustand     │      │
                 │  │   Stores      │      │
                 │  │  - tableStore │      │
                 │  │  - historyStore│     │
                 │  │  - confirmStore│     │
                 │  └───────┬───────┘      │
                 │          │              │
                 │  ┌───────┴───────┐      │
                 │  │  Engine 层     │      │
                 │  │ (Operation 模式)│     │  ← engine/operations/
                 │  │ + execute hook │      │     useOperationExecutor
                 │  └───────────────┘      │
                 └──────────────────────────┘
```

### UI 组件状态（更新）

| 组件 | 文件 | 状态 | 说明 |
|------|------|------|------|
| App | `components/App.tsx` | ✅ 已更新 | 顶栏内置 Undo/Redo + 导入/导出，移除独立 ToolBar |
| DataGrid | `components/DataGrid.tsx` | ✅ | AG Grid 表格 |
| StatusBar | `components/StatusBar.tsx` | ✅ | 底部统计栏 |
| OperationPanel | `components/OperationPanel.tsx` | ✅ | 右侧操作面板，按功能结构分组 |
| ConfirmDialog | `components/ConfirmDialog.tsx` | ✅ | 全局确认弹窗，监听 confirmStore |
| HistorySidebar | `components/HistorySidebar.tsx` | ❌ 待建 | 操作历史时间线面板 |
| **ToolBar** | `components/ToolBar.tsx` | ❌ 已弃用 | 操作按钮已迁移至 OperationPanel（保留文件，不再被 App 引用） |

**Workspace 概念预留（Phase 2）：**
- `Workspace.tables: Map<string, NormalizedTable>`（类型已定义）
- 跨表操作在当前单表模式下不启用

---

## 四、核心类型

> 所有类型定义在 `engine/types.ts`，这是唯一标准。
> `types/index.ts` 仅保留 ImportOptions/ExportOptions 等工具类型，主链路已不依赖。

```typescript
type ColumnType = 'text' | 'integer' | 'float' | 'currency' | 'percentage' | 'date' | 'time' | 'datetime'
type SemanticTag = 'phone' | 'email' | 'idcard' | 'bankcard'

interface Column {
  id: string
  name: string
  dtype: ColumnType
  semanticTag?: SemanticTag
  isKey: boolean
}

interface Cell {
  value: string | number | null
  isVirtual: boolean
  sourceRef: string | null
}

interface Row {
  id: number
  isVirtual: boolean
  cells: Cell[]
}

interface NormalizedTable {
  columns: Column[]
  rows: Row[]
  originalFileName: string
  sheetName: string
}
```

### 数据流（已统一）

```
文件 → 导入器(parseXLSX/parseCSV) → NormalizedTable → tableStore → AG Grid 展示
                                            ↓
                                     Engine Operation.execute()
                                            ↓
                                   返回新的 NormalizedTable → tableStore.updateTable()
                                            ↓
                                    导出器(exportToXLSX/exportToCSV) → 文件
```

---

## 五、已完成的模块

### 5.1 导入模块 ✅
| 文件 | 功能 |
|------|------|
| `importers/xlsxImporter.ts` | `parseXLSX(buffer, fileName) → NormalizedTable` |
| `importers/csvImporter.ts` | `parseCSV(text, fileName, options) → NormalizedTable` |

### 5.2 导出模块 ✅
| 文件 | 功能 |
|------|------|
| `exporters/xlsxExporter.ts` | `exportToXLSX(table) → ArrayBuffer` |
| `exporters/csvExporter.ts` | `exportToCSV(table, options) → string` |
| `utils/saveFile.ts` | `saveFile()` — Tauri 原生保存对话框 + 浏览器下载 |

### 5.3 操作引擎 ✅ — Command Pattern
```typescript
abstract class BaseOperation {
  abstract readonly type: string
  abstract readonly label: string
  abstract readonly detail: string
  abstract execute(table: NormalizedTable): NormalizedTable
  abstract undo(table: NormalizedTable): NormalizedTable
  abstract serialize(): SerializedOperation
  protected cloneTable(table): NormalizedTable  // structuredClone
}
```

已实现的操作（5个）：

| 操作类 | 文件 | 功能 | Phase |
|--------|------|------|-------|
| `FillDownOp` | `engine/operations/FillDownOp.ts` | 合并单元格向下填充 | P1 |
| `ExplodeOp` | `engine/operations/ExplodeOp.ts` | 按分隔符/换行符展开行 | P1 |
| `TrimOp` | `engine/operations/TrimOp.ts` | 去前后空格 | P1 ✅ 新增 |
| `CleanInvisibleOp` | `engine/operations/CleanInvisibleOp.ts` | 清除不可见字符 | P1 ✅ 新增 |
| `CollapseOp` | `engine/operations/CollapseOp.ts` | 按列合并（FillDown 逆操作） | P1 ✅ 新增 |

操作注册表：`engine/operations/registry.ts`

### 5.4 状态管理 ✅
- **tableStore** (`stores/tableStore.ts`)：当前表格 NormalizedTable + loading/error 状态
- **historyStore** (`stores/historyStore.ts`)：undo/redo 栈（存序列化 Operation，不存全量快照）
- **confirmStore** (`stores/confirmStore.ts`)：全局确认弹窗状态 ✅ 新增

### 5.5 UI 组件 ✅
| 组件 | 文件 | 状态 |
|------|------|------|
| App | `App.tsx` | ✅ 顶栏内置 Undo/Redo + 导入/导出 |
| DataGrid | `components/DataGrid.tsx` | ✅ |
| StatusBar | `components/StatusBar.tsx` | ✅ |
| OperationPanel | `components/OperationPanel.tsx` | ✅ 新增，按功能结构分组 |
| ConfirmDialog | `components/ConfirmDialog.tsx` | ✅ 新增 |
| ExportConfirmModal | `App.tsx` 内联 | ✅ 导出格式选择弹窗 |

### 5.6 Hooks ✅
- `useOperationExecutor` (`hooks/useOperationExecutor.ts`)：execute/undo/redo 三合一

### 5.7 Tauri 集成 ✅
- `src-tauri/src/lib.rs`：仅窗口管理
- `utils/saveFile.ts`：原生保存对话框

---

## 六、未完成的模块（按功能树 A-Z）

### 🔴 A. 导入增强
| 编号 | 功能 | 说明 |
|------|------|------|
| A2 | 合并单元格自动检测 + Unmerge + FillDown | 导入时自动展开 |
| A3 | 智能多值列检测 | 采样前 100 行，扫描分隔符出现率 |
| A4 | 手动指定分隔规则 | 修正自动检测误判 |
| A5 | 多工作表选择 | Phase 1 选 Sheet，Phase 2 批量 |
| A6 | 导入错误报告 | 损坏文件/格式错误/跳过行 |
| A7 | 导入上限性能警告 | 超 5 万行弹提示，不设硬上限 |

### 🔴 B. 展开操作
| 编号 | 功能 | Engine | UI |
|------|------|--------|----|
| B1 | 按列展开（分隔符拆多行） | ✅ ExplodeOp | ✅ OperationPanel |
| B2 | 含换行符展开 | ✅ ExplodeOp | ✅ OperationPanel |
| B3 | 多列同步展开（按位置匹配） | ❌ | ❌ |
| B4 | 合并单元格展开 | ✅ FillDownOp | ✅ OperationPanel |
| B5 | 展开预览 | ❌ | ❌ |
| B6 | 按规则展开（固定长度/正则） | ❌ Phase 3 | ❌ |

### 🔴 C. 合并操作
| 编号 | 功能 | 说明 |
|------|------|------|
| C1 | 按列合并 | ✅ CollapseOp（Engine 完成，UI 入口在 OperationPanel） |
| C2 | 选中行合并 | ❌ |
| C3 | 多列聚合合并 | ❌ |
| C4 | 聚合计算合并 | ❌ Phase 3 |

### 🔴 D. 列操作
| 编号 | 功能 |
|------|------|
| D1 | 添加列 |
| D2 | 删除列 |
| D3 | 重命名列 |
| D4 | 重新排序列 |
| D5 | 拆分列（Text to Columns） |
| D6 | 合并列（Concatenate） |
| D7 | 列类型转换 + 语义标签设置 |

### 🔴 E. 行操作
| 编号 | 功能 |
|------|------|
| E1 | 筛选（条件/正则/模糊） |
| E2 | 排序（单列/多列） |
| E3 | 去重（按列去重） |
| E4 | 删除空行/填充空白 |

### 🔴 F. 数据清洗
| 编号 | 功能 | 状态 |
|------|------|------|
| F1 | Trim（去前后空格） | ✅ TrimOp |
| F2 | 清除不可见字符 | ✅ CleanInvisibleOp |
| F3 | 大小写/全角半角统一 | ❌ |
| F4 | 正则替换（带实时预览） | ❌ |
| F5 | 模糊分组（拼写聚类） | ❌ Phase 3 |

### 🔴 G. 操作管理
| 编号 | 功能 | 状态 |
|------|------|------|
| G1 | Undo/Redo（Ctrl+Z/Ctrl+Y） | ✅ 顶栏内置 |
| G2 | 侧边栏操作历史时间线 | ❌ |
| G3 | 导出操作脚本 | Phase 4 |

### 🟡 H. 导出增强
| 编号 | 功能 | 状态 |
|------|------|------|
| H1 | 导出 .xlsx | ✅（带格式选择弹窗） |
| H2 | 导出 .csv | ✅（带格式选择弹窗） |
| H3 | 清除文档元数据 | ❌ |
| H4 | 批量导出 | ❌ Phase 2 |

### 🟡 I. 安全
| 编号 | 功能 |
|------|------|
| I2 | 数据脱敏 |
| I3 | 密码加密导出 |
| I4 | 操作审计日志 |

### 🔵 J. 公式计算引擎（Phase 3）
| 编号 | 功能 |
|------|------|
| J1 | 时间计算（工时/日期差/日期加减，含 DayOffProfile） |
| J2 | 身份证解析（生日/性别/年龄） |
| J3 | 文本提取（手机号/邮箱/数字） |

### 🔵 K. 跨表操作（Phase 2）
| 编号 | 功能 |
|------|------|
| K1 | 表连接（VLOOKUP/XLOOKUP 替代） |
| K2 | 表合并（多表纵向拼接） |
| K3 | 表拆分（按列值拆多个文件） |

### 🔵 L. 工作区管理（Phase 2）
| 编号 | 功能 |
|------|------|
| L1 | 多表同开（标签页切换） |
| L2 | 表名管理 |
| L3 | 跨表引用关系图 |

### 🟡 Z. 辅助功能
| 编号 | 功能 | 状态 |
|------|------|------|
| Z1 | 统计信息面板 | ⚠️ StatusBar 简版 |
| Z2 | 快捷键覆盖 | ✅ Ctrl+Z/Y + 导出弹窗 |
| Z3 | 帮助内嵌 | ❌ |

---

## 七、开发阶段规划

### Phase 1 — MVP（当前 ✅ 核心骨架完成）
- [x] 导入（.xlsx + .csv）
- [x] 导出（.xlsx + .csv，带格式选择弹窗）
- [x] 操作引擎（Command Pattern + 5 个 Operation）
- [x] Undo/Redo（顶栏内置）
- [x] OperationPanel（按功能分组，5 个操作入口）
- [x] ConfirmDialog（全局确认弹窗）
- [ ] 展开预览（B5）
- [ ] 导入增强：合并单元格检测（A2）、多值列检测（A3/A4）

### Phase 2 — 基础操作
合并（C2-C4）、列操作（D1-D7）、行操作（E1-E4）、清洗（F3-F4）、
多表支持（A5）、跨表/工作区（K+L）、安全（I2-I4）、历史可视化（G2）

### Phase 3 — 高级功能
公式引擎（J1-J3）、规则展开（B6）、聚合合并（C4）、模糊分组（F5）

### Phase 4 — 远期
操作脚本导出重放、大文件流式+分页加载、帮助文档

---

## 八、当前下一步任务（2026-05-26 更新）

### 立即推进（按优先级）

| 优先级 | 任务 | 分类 | 说明 |
|--------|------|------|------|
| 🔴 1 | HistorySidebar（操作历史面板） | UI | 已有 historyStore，只需 UI；展示操作时间线，支持点击回退 |
| 🔴 2 | RegexReplaceOp | Engine | 正则替换，对应 F4，CleanInvisibleOp 的泛化版本 |
| 🔴 3 | 全角半角统一（F3） | Engine | 标点符号、字母、数字的全角→半角转换 |
| 🔴 4 | 列拆分（D5 Text to Columns） | Engine+UI | 按分隔符/固定宽度拆分列 |
| 🟡 5 | 展开预览（B5） | UI | 执行 ExplodeOp 前预览前 20 行 |
| 🟡 6 | 行去重（E3） | Engine+UI | 按指定列去重 |
| 🟡 7 | 列重命名（D3） | UI | AG Grid 表头双击编辑 |

### 开发策略
- Engine 层 Operation 优先：写完就能测试，不依赖 UI
- Engine 完成后立即在 OperationPanel 加入口按钮
- UI 组件（如 HistorySidebar）可与 Engine 并行推进

---

## 九、开发规范

### 文件注释格式
```typescript
/**
 * 模块名 — 一句话描述
 *
 * 职责：详细说明该模块的职责
 *
 * 数据流（如涉及）：
 *   Step 1 → Step 2 → Step 3
 */
```

### Operation 类实现标准
```typescript
export class XxxOp extends BaseOperation {
  readonly type = "xxx";
  readonly label = "操作名称";
  readonly detail = "操作参数描述";

  execute(table: NormalizedTable): NormalizedTable {
    const result = this.cloneTable(table);
    // ... 修改 result
    return result;
  }

  undo(table: NormalizedTable): NormalizedTable {
    // ... 逆向操作
    return table;
  }

  serialize(): SerializedOperation {
    return { type: this.type, params: { ... } };
  }
}
```

### Store 标准
```typescript
import { create } from "zustand";
interface XxxState { /* ... */ }
export const useXxxStore = create<XxxState>((set, get) => ({ /* ... */ }));
```

### Component 标准
```typescript
/**
 * 组件名 — 功能描述
 */
export function XxxComponent({ prop1, prop2 }: Props) { /* ... */ }
```

---

## 十、文件树（当前实际 2026-05-26）

```
excel-normalizer/
├── src/
│   ├── App.tsx                    ✅ 根组件（顶栏内置 Undo/Redo + 导入/导出）
│   ├── components/
│   │   ├── DataGrid.tsx           ✅ AG Grid 表格
│   │   ├── StatusBar.tsx          ✅ 底部统计栏
│   │   ├── OperationPanel.tsx     ✅ 右侧操作面板（按功能结构分组）
│   │   ├── ConfirmDialog.tsx      ✅ 全局确认弹窗
│   │   └── ToolBar.tsx            ⚠️ 已弃用（不再被 App 引用）
│   ├── engine/
│   │   ├── index.ts               ✅ 入口
│   │   ├── types.ts               ✅ Engine 核心类型
│   │   ├── protocol.ts            ✅ Worker 协议（预留）
│   │   ├── engine.ts              ✅ Engine 类
│   │   ├── workerEntry.ts         ✅ Worker 入口（未启用）
│   │   └── operations/
│   │       ├── index.ts           ✅ 汇总 + 导出
│   │       ├── Operation.ts       ✅ 基类
│   │       ├── registry.ts        ✅ 注册表
│   │       ├── FillDownOp.ts      ✅ 合并单元格展开
│   │       ├── ExplodeOp.ts       ✅ 按分隔符展开
│   │       ├── TrimOp.ts          ✅ 去前后空格（新增）
│   │       ├── CleanInvisibleOp.ts✅ 清除不可见字符（新增）
│   │       └── CollapseOp.ts      ✅ 按列合并（新增）
│   ├── stores/
│   │   ├── tableStore.ts          ✅
│   │   ├── historyStore.ts        ✅
│   │   └── confirmStore.ts        ✅ 确认弹窗状态（新增）
│   ├── hooks/
│   │   └── useOperationExecutor.ts ✅
│   ├── importers/
│   │   ├── xlsxImporter.ts        ✅
│   │   └── csvImporter.ts         ✅
│   ├── exporters/
│   │   ├── xlsxExporter.ts        ✅
│   │   └── csvExporter.ts         ✅
│   ├── utils/
│   │   └── saveFile.ts            ✅
│   ├── types/
│   │   └── index.ts               ⚠️ 工具类型（仅保留 ImportOptions/ExportOptions）
│   └── operations/                ⚠️ 旧引擎（8 文件，待迁移后删除）
├── src-tauri/
│   ├── src/lib.rs                 ✅
│   ├── Cargo.toml                 ✅
│   └── capabilities/              ✅
└── tests/
    └── placeholder.test.ts        ⚠️ 占位
```

### 待清理文件

| 文件/目录 | 处理方式 |
|-----------|----------|
| `src/operations/` | 旧引擎目录，逐步删除（功能已迁移至 engine/operations/） |
| `src/components/ToolBar.tsx` | 已弃用，可删除（操作入口在 OperationPanel + 顶栏） |
| `src/types/index.ts` | 仅保留工具类型，旧 NormalizedTable 定义已移至 engine/types.ts |

---

## 十一、Worker 通信协议（预留，当前未启用）

```
// main → worker
type MainMessage =
  | { type: 'LOAD_FILE';     buffer: ArrayBuffer; format: 'xlsx' | 'csv' }
  | { type: 'EXECUTE_OP';    op: SerializedOperation }
  | { type: 'UNDO' }
  | { type: 'REDO' }
  | { type: 'EXPORT';        format: 'xlsx' | 'csv' }
  | { type: 'PREVIEW';       op: SerializedOperation; rows: number }

// worker → main
type WorkerMessage =
  | { type: 'TABLE_LOADED';   table: NormalizedTable; stats: TableStats }
  | { type: 'OP_EXECUTED';    table: NormalizedTable }
  | { type: 'OP_UNDONE';      table: NormalizedTable }
  | { type: 'OP_REDONE';      table: NormalizedTable }
  | { type: 'PREVIEW_RESULT'; preview: PreviewRows; rowCountChange: string }
  | { type: 'EXPORT_READY';   buffer: ArrayBuffer; format: 'xlsx' | 'csv' }
  | { type: 'PROGRESS';       current: number; total: number }
  | { type: 'ERROR';          message: string; stack?: string }
```

> 当前主链路在主线程直接执行，Worker 路径待后续重构时启用。